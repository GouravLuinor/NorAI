# NorAI — Project Progress (living current-state doc)

> Condensed from the original per-week changelog (recoverable from git history). Update this file whenever you make a notable pipeline/backend/frontend change. The single always-loaded instruction file is `AGENTS.md`; this doc is the project's current-state snapshot.

## Vision

NorAI turns a long lecture video into a complete study experience: structured study notes, revision notes / cheat sheets, assessments, flashcards, and a lecture-grounded AI tutor (RAG over the lecture's text + screenshots).

## Current Architecture

```
Video (YouTube / upload / Drive)
  → Ingestion (16kHz mono audio extract, metadata)
  → Transcription (parallel Gemini 3.1 Flash-Lite API with 18m chunks, timestamped segments)
  → Chunking (segment→chunk, timestamps preserved)
  → Knowledge Extraction (transcript → structured knowledge objects)
  → Frame Extraction + Scene Detection (parallel with transcription/chunking)
  → Chunk ↔ Screenshot Mapping
  → Outline Generation
  → Visual Knowledge Extraction (per-chapter, batched)
  → Knowledge Merging
  → Chapter Building
  → Screenshot Selection
  → Chapter Artifacts (study notes + revision + assessment + flashcards, 1 call/chapter)
  → Tutor + Screenshot Indexing (Chroma)
  → Cleanup
```

- **Backend**: FastAPI (`backend/main.py`, all routes inline) + `backend/orchestrator.py` runs the 18-stage pipeline synchronously in a `backend/jobs.py` worker thread; progress persists to the DB and is polled by the frontend (no SSE).
- **Tutor**: LangGraph RAG (`tutor/`) — per-lecture graph isolation, persistent multi-thread memory (SQLite checkpoints), parallel text+screenshot retrieval, tool calling, quiz workflows, flashcards.
- **Frontend**: React 19 + TypeScript + Vite, three-panel workspace (doc pane / chat / AI panel), Zustand stores, Tailwind v4. Design theme: **"Architect's Sketchbook"** (warm vellum light default + Prussian-blue dark, Space Grotesk + Newsreader, red-pencil accents).

## Model & config truth (source of truth: root `config.py`)

- `MODEL_NAME = "gemini-3.1-flash-lite"`, `TEMPERATURE = 0.4`, `GEMINI_API_KEY` from `.env` (required).
- Pricing: **$0.25 input / $1.50 output / $0.025 cached input** per 1M tokens.
- Transcription: **`NORAI_TRANSCRIPTION_BACKEND = "gemini"`** with 18-minute parallel chunking (`gemini-3.1-flash-lite`), yielding 9.2x speedup (53s for 71m lecture vs 498s CPU) with 0% server CPU. Optional local fallback via `NORAI_TRANSCRIPTION_BACKEND = "whisper"`.
- Embeddings: `gemini-embedding-2` (768 dims) via custom Chroma embedding fn in `tutor/embedding.py` (`tutor/retrieval_config.py`). No `task_type=` arg; instructions go in the prompt.
- Chroma store: `outputs/tutor/chroma` (override `NORAI_CHROMA_DIR`).
- Free-tier ceiling: **15 RPM / 500 RPD** — pipeline consolidated from ~128 → ~35 LLM calls per lecture. Transient 15 RPM spikes are silently retried by internal exponential backoff; 500 RPD daily quota exhaustion fast-fails with structured 429 (`limit_type: rpd`, `retry_after_seconds`) and triggers global UI countdown banner to Midnight US Pacific Time.
- Trial quota: **45 minutes** default free trial processing credit (increased from 15 min).
- Authentication: **Mandatory user auth** across all routes and API endpoints. Guest mode and `X-Guest-Id` headers removed. Public read-only preview is preserved for 3 pre-loaded demo lectures (`DEMO_LECTURE_IDS`), with interactive AI Tutor locked behind sign-up.
- Monetization: **Soft-disabled payments** non-destructively via `ENABLE_PAYMENTS` / `VITE_ENABLE_PAYMENTS` flags. UI entry points (Pricing links, Upgrade buttons) hidden and routes redirected.

## Output layout

All artifacts land under `outputs/<lecture-id>/` (notes, revision, assessment, screenshots, flashcards, pdfs, tutor checkpoints, chroma, `backend.log`). Gitignored / throwaway.

- **End-to-End Code Review & Hardening Audit (Phases 1–4 complete)**:
  - **Tutor & Streaming Chat**: Fixed zombie-thread false positive rejection in `_thread_exists()`, added eager LangGraph checkpointer initialization (`checkpointer.setup()`), added `asyncio`/`uuid` and guaranteed non-null UUIDs on LangGraph memory messages for safe checkpoint pruning, added `_content_text` parser for Gemini thinking block lists, and surfaced real backend error messages in frontend chat UI.
  - **Frontend State & Lifecycles**: Fixed `loadThreadMessages` skipping default thread in `Sidebar.tsx` and `useThreadStore.ts`, added generation counters to `useChapterStore` and `useVideoStore` to drop stale out-of-order async responses, upgraded `useVideoStore.load` to parallel `Promise.all` with individual catch isolation, and stabilized `useEffect` dependencies across billing/courses/usage pages.
  - **Job Queue Durability**: Preserved uploaded video files during retryable failures in `backend/jobs.py` (`attempts < PIPELINE_MAX_ATTEMPTS`), replaced premature `orchestrator.py` error broadcasting with `jobs.py`'s `stage = "retrying"` progress countdown, and guarded `_executor.submit` against `NoneType`.
  - **Security & Concurrency**: Verified static file allowlists, video upload streaming caps, pre-flight quota enforcement, JWT verification, and LRU graph cache eviction. 100% green across all 38 backend/tutor test suites + 73 Vitest + oxlint (0 errors).
- **Pipeline**: 18-stage orchestrator with DB-persisted progress (polled; no SSE); concurrent transcription/chunking + frame/scene extraction; graceful degradation (partial-content marker) on retry failures.
- **Per-lecture isolation**: lecture registry (`backend/lecture_registry.py`), per-lecture tutor graphs + checkpoints + Chroma indexes + thread history; backward-compatible default/global tutor mode.
- **Tutor**: grounded Q&A with citations + timestamps, chapter-aware routing, query rewriting, confidence-aware answers, persistent multi-thread conversations, highlight-and-ask, references panel, screenshot previews.
- **Resources**: study notes, revision notes, assessments, quiz engine (start/answer/evaluate), flashcards, chapter screenshots, PDF export (notes/revision/guide/concepts).
- **Frontend**: workspace with resizable panels (keyboard-accessible), doc tabs (notes/revision/assessment), Quiz mode + flashcard "Cards" mode in the AI panel, search, print/PDF, dark mode toggle, math rendering (KaTeX), shortcuts modal.

## Recent changes

- **Tutor Chat State Refresh & Multi-User Isolation on Demo Lectures (2026-09-05).**
  - **Thread & Chat Partitioning (`backend/routers/tutor.py`)**: Gated and partitioned LangGraph checkpoints per-user on shared demo lectures (`f"{user_scope}:{thread_id}"` where `user_scope` is `user.id` or `'guest'`), preventing conversations from leaking across different user accounts.
  - **Frontend Storage Scoping (`frontend/src/lib/threadStorage.ts`)**: Scoped all localStorage keys by user ID (`norai-threads-${userScope}-${lectureId}`) so that switching accounts or signing up on the same browser gives a clean slate with no stale threads or labels.
  - **Auth State Synchronization (`frontend/src/stores/useAuthStore.ts`, `Sidebar.tsx`)**: Wired `useThreadStore` reset and re-fetch into `onAuthStateChange` and `Sidebar` effect dependencies so newly registered users immediately see 1 single thread ("Thread 1") and 0 messages.
  - **Seed & DB Hygiene**: Cleaned leftover test checkpoints from `seed_data/506dd685...` and `outputs/<demo_id>/tutor/checkpoints.sqlite`. Verified across all 3 demo lectures and backend contract suites (20/20 passed).

- **Phase 5 architecture & simplification (2026-08-22, audit-driven).** `backend/main.py` shrank 3175 → ~860 lines by extracting every domain cluster into `backend/routers/` (`webhooks`, `billing`, `courses`, `quiz`, `content`, `tutor`) mounted prefix-free (paths unchanged). Shared helpers moved to `backend/access.py` (access checks, guest/demo caps, persona hardening, tutor-usage flushers) and `backend/lecture_db.py` (`_db` + all DDL-ensure helpers); the concept-map graph builder became a pure function in `backend/concept_map.py`. Gemini client construction + retry/RPM backoff collapsed into one factory, `backend/gemini_client.py`, used across tutor/, extract/, transcription/, notes/, visual/. Quiz API payloads are now typed (`Question` / `AnsweredQuestion` Pydantic models mirroring the frontend interface; unknown fields tolerated, required ones enforced). All pipeline-stage functions require explicit `output_dir` (no silent `"outputs"` default). Dead code deleted (`generate_pdfs.py`, unused `init_db`, dead chapter-summary node). Frontend dedupe: shared `useAskNora` hook, `lib/id.ts` id generator, consolidated `stripSources`. Verified: suite 42/42, oxlint clean, vitest 73/73, build clean.

- **Device-scoped guest auth + YouTube PO-token provider (2026-08-17).** Two production fixes targeting Render:
  - **Guest processing without any Bearer token.** Visitors can now process lectures under the free-trial quota (15 min) without signing in or relying on Supabase anonymous sign-in. The frontend mints a persistent `X-Guest-Id` (localStorage UUID) attached to every request (`authHeaders.ts`); the backend resolves it in `get_current_user_optional` (real Bearer token still wins; dev access still keeps its dev-user escape hatch) to an anonymous `User` + 15-min trial `Subscription`. `/process`, `/billing`, `/quota`, `/lectures` all work for guests automatically since they flow through `get_current_user(_optional)`. "Continue as Guest" in `AuthModal` no longer calls `signInAnonymously()` (fragile) — it just closes the modal and navigates to `/app`. Email + Google sign-in flows unchanged. Tests updated (`test_api_contract_offline.py`, `test_billing_quota.py`) + new `test_pot_provider.py`.
  - **Permanent YouTube download fix via PO tokens.** The 5-guess client-spoofing fallback chain was collapsed to 3 principled tiers (Web+POT → TV/embedded → Android). The Docker image now ships the `bgutil-pot` Rust binary + matching yt-dlp plugin (pinned `v0.8.1`, verified asset names via GitHub API); `scripts/entrypoint.sh` starts the POT server on :4416 then uvicorn, and `ingest.py` health-checks `NORAI_POT_SERVER_URL/ping` before adding the `youtubepot-bgutilhttp:base_url` extractor arg. Verified end-to-end: with binary on PATH + plugin in `~/.config/yt-dlp/plugins`, `yt-dlp --verbose` reports `PO Token Providers: bgutil:http-0.8.1 (external)` and extraction proceeds. Cookie support retained (age-restricted / logged-in videos). Honest caveat: no server-side method is 100% against YouTube; upload-direct remains the manual escape hatch.
  - **Verification**: `scripts/run-tests.sh` **40/40**, `npm run lint` 0, `npm run test` **73/73**, `npm run build` clean. Live dev smoke: `X-Guest-Id: test` → `/quota` anonymous trial (15 min), `/billing` 200, `/process` bad-url → 400 (not 401).

- **Seeded Public Demo Workspaces, Docker Startup Hydration & Pre-Deployment Phase 1 Hardening (2026-08-16).**
  - **3 Curated Educational Demo Workspaces**: Pre-processed and bundled three diverse, high-impact demo lectures into `seed_data/` (36 MB total):
    1. *Foundations of Neural Networks & Deep Learning* (`ab648382-638f-4dde-b7c1-4007a2e638bb`): 4 chapters, KaTeX parameterization derivations, 3D flashcards.
    2. *Foundations of Economic Thinking: Incentives and Opportunity Cost* (`e54d7376-0e7b-472a-9ca6-9b21ad0b2710`): 6 chapters, price distortion tables, fiscal policy analysis, Socratic AI Tutor.
    3. *The Rise of Open-Weights Models and Local Deployment* (`506dd685-05f9-43df-8d09-5b944c7392f5`): 3 chapters, local inference VRAM formulas, dynamic quantization benchmarks.
  - **Zero-Configuration Docker Container Hydration (`backend/main.py`)**: Implemented `sync_demo_seed_data()` in FastAPI `lifespan` startup to copy `seed_data/` into `outputs/` and merge `seed_data/lectures.json` automatically upon container launch on Render or Hostinger VPS.
  - **Unrestricted Public Access (`backend/main.py`)**: Added `DEMO_LECTURE_IDS` check in `ensure_lecture_access()` allowing visitors to explore full notes, quizzes, flashcards, and Nora AI Tutor without authentication or quota deductions.
  - **Frontend Landing & Upload Enhancements (`frontend/src/pages/LandingPage.tsx`, `UploadPage.tsx`)**: Added an "Explore 3 Live Demo Workspaces" interactive card showcase on the Landing Page and `Demo` badges on recent lectures in the Upload Page.
  - **Phase 1 Production Hardening**:
    - Gated `/docs`, `/redoc`, and `/openapi.json` behind `NORAI_ENV=production`.
    - Added configurable production CORS origins via `NORAI_ALLOWED_ORIGINS` and automatic `RENDER_EXTERNAL_URL` staging allowlist.
    - Updated `.env.example` with `NORAI_ENV`, `NORAI_ALLOWED_ORIGINS`, and Razorpay integration keys.
  - **YouTube Ingestion & Frame Extraction Resilience (`ingest/ingest.py`, `visual/extract_frames.py`)**:
    - Stripped `ios` client from yt-dlp to avoid GVS PO token 403 Forbidden errors; added a 4-tier progressive client fallback (`android_vr` → `android` → `mweb` → `web`).
    - Added preference for H.264 streams (`[vcodec^=avc1]`) + automatic FFmpeg transcoding to H.264 MP4 to avoid OpenCV AV1 decoding crashes (`Saved 0 frames`).
    - Added native FFmpeg frame extractor fallback `_extract_frames_ffmpeg` in `visual/extract_frames.py`.
  - **Verification**: `oxlint` (0 errors), `npm run build` (built in 870ms, 0 errors), backend API smoke tests on all 3 demo workspaces (`/outline`, `/notes/1`, `/assessment/1`, `/flashcards/1`, `/chat` — all passed).

- **Pipeline Polling Resilience & DB Connection Hardening (2026-08-15).** Resolved 60s status-polling stalls and unhandled 500 errors when remote database connectivity drops or experiences DNS timeouts:
  - **Fast Connection Timeouts (`backend/db/database.py`, `backend/usage.py`)**: Added dynamic `get_engine_kwargs(url)` passing `connect_args={"timeout": 5, "command_timeout": 10}` for asyncpg/PostgreSQL, preventing queries from freezing asyncio event loop threads for 60s when `getaddrinfo` hangs.
  - **In-Memory Live Progress Tracking (`backend/jobs.py`)**: Added thread-safe `_live_progress` cache updated synchronously on every stage transition in `update_job_progress()` and `_set_status()`, enabling 0ms latency status responses regardless of DB write backpressure.
  - **Multi-Tier Status Fallback (`backend/jobs.py`, `backend/main.py`)**: `get_job_status()` now checks disk artifacts (`check_disk_completed`), live in-memory progress, database, and file registry sequentially. Guarded `@app.get("/process/{task_id}/status")` against unhandled DB exceptions so polling never returns 500 errors.
  - **Frontend Completion Handling (`frontend/src/pages/ProcessingPage.tsx`)**: Updated `processEventData()` to trigger full completion when `data.progress >= 100` alongside `stage === 'complete'`.
  - **Verification**: `test_jobs_restart.py` (19/19 passed), `test_api_contract_offline.py` (15/15 passed), `oxlint` (0 errors), `npm run build` (clean). Instant (<10ms) status responses verified.

- **Chief-reviewer pass + fixes on the dynamic-scaling work (2026-08-15, commit `b66db59`).** Full audit of the Content-Adaptive Scaling + screenshot-selection + citation commits (findings in `fix.md`; 2 CRIT / 5 HIGH / 6 MED / 9 LOW), all fixed and verified (38/38 backend, 73/73 Vitest, oxlint, `tsc`/vite build). Highlights: the outline proto-payload was slicing `lecture_notes` to **5 characters** (`[:5]` on a `str`) and `concepts[:10]` — both removed, so the outline LLM now genuinely receives the full chunk content; the visual extractor's blanket fallback that re-scored webcam frames as slides when `source_screenshots` was empty is gone (empty list → frames marked decorative, `importance=1`); the non-monotonic `*2` importance stretch (5→10 but 6→6) removed in favor of the raw 1–10 score; chapter counting aligned to one tiered rule (≤12 chunks→4, 13–25→6, 26–45→9, 46+→14) and the LLM's per-chapter chunk ranges are now honored via `_valid_chapter_ranges()` (even-split fallback); `stripSources` no longer truncates prose containing the word "Sources"; mobile citation clicks now switch to the doc tab (`norai:show-doc`); `ProcessingPage` clears stale checkmarks on retry re-runs. See `fix.md` for the full log.

- **Content-Adaptive Dynamic Scaling & Ingestion Hardening (Phases 1–4 complete & live benchmarked).** Replaced fixed-envelope pipeline constraints with dynamic, content-density-aware scaling across chunks, curriculum outlines, and structured note generation:
  - **Dynamic Chunking (`config.py`, `chunking/chunk.py`, `backend/estimator.py`)**: Reduced `MAX_SEGMENTS_PER_CHUNK` from 60 to 30 (~2.8 mins max per chunk) to preserve granular sub-topics in dense multi-service courses without squashing 6 minutes into one chunk. Set `TARGET_MAX_CHUNKS = 40` and aligned `estimate_pipeline()` to the outline generator's tiered chapter cap (≤4 / 6 / 9 / 14).
  - **Untruncated Curriculum Outline Graph (`notes/outline_prompts.py`, `notes/outline_generator.py`)**: Removed rigid "strictly 3 to 6 chapters" rule, implementing dynamic range scaling (2–4 chapters for ≤15m up to 8–14 chapters for ≥65m). Removed `[:1000]` character truncation on summaries and `[:5]` / `[:10]` topic slicing (fully, after the 2026-08-15 review pass). The outline LLM now receives complete chunk summaries, topics, and concept graphs. Increased `max_output_tokens` to 4096. Per-chapter chunk ranges returned by the LLM are validated and honored (`_valid_chapter_ranges`, even-split fallback).
  - **Depth-Proportional Chapter Synthesis (`notes/notes_prompt.py`, `notes/notes_generator.py`)**: Replaced flat 300–700 word limit with `TARGET DEPTH & DENSITY` (scaling from ~400–700 words for narrow topics to ~1,500–2,500 words across 4–8 structured card sections for dense architectures).
  - **YouTube Multi-Client Ingestion (`ingest/ingest.py`)**: Configured `player_client: ["ios", "android", "web"]`, `retries=10`, and `fragment_retries=10` in `yt-dlp` options, permanently resolving 403 Forbidden errors.
  - **Screenshot selection hardening (incl. 2026-08-15 review fixes)**: webcam/talking-head frames can no longer re-enter notes (empty `source_screenshots` → frames marked decorative; importance scored on the raw 1–10 scale; blur/occlusion bounds enforced in the quality bar and fallback; missing Pass-1 scores retried). Cross-chapter citation clicks work on mobile and no longer match prose "Sources" text.
  - **Benchmarked Live**: Verified on 8-min DP lecture (3 cohesive chapters, 1,428 clean words, 0 filler) vs 71-min AWS course (7 dedicated architectural chapters, 3,562 words, 21 assessment questions, 21 flashcards, 33 indexed tutor chunks, 12 diagram screenshots).
  - **Verification**: 38/38 backend/tutor test suites passing, 73/73 Vitest frontend unit tests passing, `oxlint` 0 errors, `tsc -b && vite build` clean.

- **UI/UX audit execution follow-through + P6.4 combined (commit `8c4cd74`, pushed to `fix/threads-and-pdf`).** Closed out the remaining audit-action items from `UI_UX_AUDIT_REPORT.md` and shipped them together with P6.4 in one verified commit:
  - **D1 ShareModal data source fixed** — `ShareModal` was calling a non-existent `/share` endpoint; added a real `GET /lectures/{id}/share` (returns the existing share link or 404) so the modal shows the live link/toggle state instead of always minting.
  - **D2 UploadPage custom Select** — replaced the native `<select>` on UploadPage ("Add to course") with the blueprint-styled `Select.tsx` for visual + keyboard consistency with Sidebar.
  - **B1–B3 skeleton loading everywhere** — `SkeletonCard`/`NotesSkeleton` extended to Revision, Study Guide, Mind map, and Assessment views, plus a quiz loading skeleton, eliminating loading layout shift (CLS) across the workspace.
  - **C1–C2 dark-theme code surfaces** — `--color-npbd` token (dark code background) applied to code blocks/`<pre>`/`Markdown` so dark mode no longer shows near-white slabs; verified against the Web Interface Guidelines.
  - **A1–A5 responsive/workspace/sidebar fixes** — mobile drawer force-expanded state, tablet icon-rail polish, and sidebar/panel edge cases across the 3-tier layout.
  - **E1 `Select.tsx` accessibility** — full ARIA combobox pattern (role, `aria-expanded`, `aria-activedescendant`, `aria-haspopup`, roving focus) + keyboard navigation, audited and green.
  - **Verification:** oxlint 0 errors; `tsc -b && vite build` clean; **70/70 Vitest**; backend **37-check `test_courses_shares.py`** (incl. the new share-GET endpoint) + **21-check `test_migrations.py`** (head `0004_courses_and_shares`); all offline suites green. P6.4 courses + closed-by-default share links shipped in the same commit (see the P6.4 entry below).

- **Frontend UI/UX Redesign & Audit Execution (Architect's Sketchbook v2 — complete, all 3 phases implemented & verified).** Fully executed the audit roadmap from `UI_UX_AUDIT_REPORT.md`:
  - **Phase 1 (Quick Wins & Fonts)**: Fixed missing Google Fonts (`Inter` & `JetBrains Mono` in `index.html`), dark theme WCAG AA text contrast (`--color-nt3`/`--color-nt4` in `index.css`), and stripped redundant mono number prefixes (`01.`, `02.`, `03.`, `03`–`07`) across `DocPanel.tsx`, `Sidebar.tsx`, and `UploadPage.tsx`.
  - **Phase 2 (Ergonomics & Responsiveness)**: Built blueprint-styled custom combobox `Select.tsx` (replacing native `<select>` in `Sidebar.tsx`), expanded column resizer hit area to 14px with visual hover grip handles in `Workspace.tsx`, and implemented full 3-tier viewport adaptivity (desktop `≥1024px` 3-panel, tablet `768px-1024px` icon rail + AI overlay drawer, mobile `<768px` single view mode with top tab switcher & slide-over drawers).
  - **Phase 3 (UX Polish & Grounded AI)**: Built `SkeletonCard.tsx` (`NotesSkeleton`) to eliminate loading layout shifts (CLS) in `NotesView.tsx`, added interactive hover quote popover cards in `CitationBox.tsx`, and upgraded `FlashcardsPanel.tsx` card flip animation to Framer Motion `motion.div` 3D spring physics (`stiffness: 280, damping: 22`).
  - **Verification**: `oxlint` 0 errors; `tsc -b && vite build` completed cleanly with zero errors.

- **P7 token-reduction sprint part 2 (output-token cost cut — done, offline-verified).** Output bills at 6× input and is 75% of pipeline cost; audit of generated fields on lecture `6af222a9-…` found two **never-consumed** fields — pure wasted output. `external_knowledge` (extract, 23% of stage output) removed from `ChunkKnowledgeModel` + `KnowledgeObject` (`extract/models.py`) and the `OUTPUT_SCHEMA`/rules (`extract/prompts.py`); `visual_summary` (visual, 17% of stage output) removed from `VisualObjectItem`, `create_empty_visual_object`, `required_fields`, and the chapter-batch prompt (`visual/visual_extractor.py`, `visual/visual_prompts.py`); dead writes dropped from both merge paths (`extract/merger.py`). Because they were the `response_schema` fields, the API simply stops emitting them — no truncation risk; merger `.get(..., default)` reads mean old cached chunk files still load. `ocr_text` (visual, 46%) deliberately kept — it feeds the screenshot selector via `visual_analysis_ch*.json`. Also added `max_output_tokens` bounds (only notes had one before): extract **1200** (obs max 613), visual chapter-batch **3000** (obs max 1438), outline **1000** (obs max 446) — all ~2× observed max, so typical runs are untouched but pathological responses are bounded. Expected savings ≈ **$0.0026/run (~10% of the $0.0269 baseline)**. Verification: 10/10 cached chunks re-merge cleanly with the fields absent; offline suite 37/37 green; no paid probe needed (fields provably unused, caps above observed output). Full analysis in `docs/token_reduction.md` §4.

- **P7 token-reduction sprint part 1 (prompt compression + tutor context caching — code complete + offline-verified; caching blocked live by free-tier quota).** Full plan/cost-math in `docs/token_reduction.md`. **(a) Prompt compression** (all done, 4-chars-per-token measured): `TUTOR_SYSTEM_PROMPT` 1605→1135 tok (SOCRATIC ~355), `EXTRACTION` 408→362, `VISUAL` 799→690, `NOTES` 1057→856, `OUTLINE` 257→249 — same intent, no capability loss. **(b) Gemini context caching for long tutor conversations** (Option B rolling prefix): new `tutor/cache.py` — static prefix = `system_instruction` (system prompt + persona + rolling summary) + `contents` (stable lecture context: outline + chapter notes + transcript, capped `MAX_PREFIX_CHARS=40_000`) cached per lecture; display name embeds `sha256(prefix)[:12]`; in-process registry `lecture_key → {name, hash, created}` means hot turns reuse the cache with **zero API calls**; rolling refresh deletes stale caches when the summary changes; skips gracefully below `MIN_CACHE_TOKENS` (4096) or on any API failure — a cache hiccup can never change an answer. `generate_answer_node` (now bound `output_dir` via an async closure in `tutor/graph.py`) builds the cache via `asyncio.to_thread` and, when active, sends **no SystemMessages** (LangChain would merge them into a competing `system_instruction`), only the dynamic suffix (context block + image context + recent window + question) with `cached_content` threaded through `make_chat_llm` (`tutor/llm.py` now records `cached_input_tokens` from `usage_metadata.input_token_details.cache_read`). `backend/usage_ledger.py` + `config.py` bill cached input at the cached rate ($0.03/1M vs $0.30/1M) via `model_price(cached_input_tokens=...)`; `TUTOR_CACHE_TTL_SECONDS` (env `NORAI_TUTOR_CACHE_TTL_SECONDS`, default 1800). LRU graph eviction in `backend/dependencies.py` now also purges the lecture's caches. Summary cap relaxed 5→7 sentences (a richer summary is a better cache anchor). **(c) Paid probe finding (2026-08-13, user-consented):** the API **accepts** our cache payload (role'd `contents` + `system_instruction` + `ttl`; 4096-token min satisfied at ~8.3k est tokens) but returns `429 RESOURCE_EXHAUSTED TotalCachedContentStorageTokensPerModelFreeTier … limit=0` for flash-lite on the current key — the free tier allows **zero cached-content storage**, so caching stays a dormant no-op (graceful uncached fallback proven live) until a key with cached-content quota is used; no code change needed to activate. Tests: new `tutor/test_cache.py` (23 checks, mocked client), `tutor/test_llm_usage.py` extended; full `scripts/run-tests.sh` 37/37 green. (Also fixed `_create_cache` to set `role="user"` on cached contents — the API rejects role-less cached content.)

- **P6.4 multi-lecture organization + sharing (ROADMAP.md P6.4 — shipped, offline-verified; per-course tutor contexts P6.4c deferred).** **(a) Course collections.** Migration `0004_courses_and_shares` adds `courses` (id, user_id, name, description, timestamps), `course_lectures` (PK (course_id, lecture_id), `position` — ordered membership, delete-orphans with the course) and `share_links` (id = unguessable `token_urlsafe(12)` slug PK, lecture_id, created_by, `allow_tutor_chat` bool `server_default=sa.true()`, `expires_at`, timestamps; cascades with the lecture/user). Backend CRUD: `GET/POST /courses`, `GET/PATCH/DELETE /courses/{id}` (owner-isolated via `_get_owned_course`, 404 on foreign), `POST/DELETE /courses/{id}/lectures`, `PUT /courses/{id}/lectures` reorder — with `lecture_count` via `func.count`, positions via `func.coalesce(func.max(...),0)+1`. `POST /process` gained an optional `course_id` form field that files the new lecture into the caller's course. **(b) Closed-by-default sharing (access-model change).** `ensure_lecture_access(lecture_id, user, db, require_tutor=False)` is now the single gate: anonymous → 404 unless a valid non-expired share link exists (legacy `default` stays open); authenticated → owner check, else share link; `require_tutor=True` (chat + quiz/flashcards writes) additionally demands `allow_tutor_chat`. `GET /lectures` is user-scoped when authenticated (DB rows enriched from the registry; anonymous keeps the legacy global registry list). New endpoints: `POST /lectures/{id}/share` (owner-only, create-or-refresh, returns `{slug, url}` from `NORAI_APP_URL`, default `http://localhost:5173`), `PATCH /lectures/{id}/share` (toggle `allow_tutor_chat`), `DELETE /lectures/{id}/share` (revoke — kills anonymous access instantly), `GET /share/{slug}` (public resolve → lecture_id/title/status/source_type/allow_tutor_chat, 404 when missing/expired; Python-side `expires_at` comparison normalizes naive SQLite datetimes). `/chat` now re-raises `HTTPException` from the access gate (was swallowed into a 500). **Owner-pays metering decision:** tutor cost stays metered to the lecture owner via the existing `record_tutor_turn` path (a shared `GEMINI_API_KEY` means the deployment owner always pays); `allow_tutor_chat` is the exposure control, not a billing split. **Frontend:** `useCourseStore.ts` [NEW], `CoursesPage.tsx` [NEW] (create/delete courses, expand to add/remove/open lectures, lazy route `/courses`), `ShareModal.tsx` [NEW] (create/copy link, AI-tutor toggle switch, revoke — mounted in the DocPanel top bar next to PDF), `ShareRedirect.tsx` [NEW] (`/share/:slug` resolves then navigates into the workspace; SPA+API share the path via the `/billing`-style Vite HTML-bypass proxy), UploadPage gained an optional "Add to course" select (`course_id` in the `/process` FormData), Sidebar quota card gained a Courses link, `http.ts` gained `apiPatch`/`apiPut`. **Tests:** new `backend/test_courses_shares.py` (37 checks: anonymous/owner scoping, course CRUD + isolation, share create/toggle/revoke/expire, share-GET re-fetch, tutor-gated chat/quiz/flashcards on disabled vs enabled shares, closed-by-default reads; seeds the registry to a temp path and stubs `ainvoke_tutor` so nothing hits Gemini); `backend/test_migrations.py` head moved to 0004 + course-table checks (21 checks); live `test_api_contract.py` updated for the new model (probes the `default` lecture anonymously; rating case fixed `again`→`Again`); `study-guide` gained the `/outline`-style registry fallback so `default` resolves. Verified: backend suite (migrations/courses/contract/billing/usage) green, `tsc -b && vite build` + `oxlint` (0 errors) + 70/70 Vitest green, live smoke: anon `GET /courses` 401, `GET /share/<unknown>` 404, HTML navs to `/courses` + `/share/*` serve the SPA through the Vite proxy. **Follow-up:** P6.4c per-course tutor contexts (course-scoped retrieval/graph) and exact-moment chunk-level video seeks remain (see `ROADMAP.md`). Every Gemini call now meters real tokens + estimated USD cost. `config.py` gained `MODEL_PRICING` (gemini-3.5-flash-lite $0.30/$2.50 per 1M in/out, gemini-embedding-2 $0.20/1M in, output free) with env overrides; `backend/usage_ledger.py` [NEW] is a thread-safe per-stage accumulator that also appends each call to `outputs/llm_calls.jsonl` (now with `cost_usd`). All 8 pipeline `generate_content` sites (extract, outline, visual ×2, notes ×2, screenshot_selection ×2), the embedding batch, and every tutor chat LLM call record through it. The orchestrator snapshots the ledger pre-run and diffs post-run → `record_pipeline_outcome` writes **one UsageLog row per stage** (new `model` + `calls` columns, migration `0003_usage_log_columns`); `/chat` + `/chat/stream` diff the `tutor` stage per turn and meter it to the lecture owner only (anonymous/default → file-only). New `GET /usage` (auth-optional, `period=month|today`) aggregates totals + by-stage + by-day + by-lecture from the DB and flags `is_estimated` when embedding rows are present (embedding cost = billable chars / 4, the embed API returns no token count). Frontend: `UsagePage.tsx` [NEW] (summary cards, dependency-free SVG per-day cost bar chart, per-stage cost bars, per-lecture table), lazy route at `/usage` (Vite + SPA-middware HTML-bypass like `/billing`), entry links in the Sidebar quota card and BillingPage. **Key fix:** `tutor/llm.py` read Gemini usage keys (`prompt_token_count`/`candidates_token_count`) that langchain-google-genai 4.2.5 converts to LangChain `input_tokens`/`output_tokens` — tokens were always None; now reads LangChain keys with Gemini-key fallback. Tests: `backend/test_usage_dashboard.py` (31, +async-variant), `tutor/test_llm_usage.py` (14, +astream-aggregation), `frontend/src/pages/UsagePage.test.tsx`, `backend/test_migrations.py` updated (16, now head 0003), `test_usage.py` still green (fallback path). Frontend build/lint/tests green (70/70). **Live DoD (2026-08-13):** consented pipeline on `mY3bR9qjZr4` (12.08 min) → `GET /usage` showed 18 calls / 27,276 in / 13,382 out / **$0.0269** as 5 per-stage rows (extract 10, notes 3, visual 3, outline 1, embed 1); two real tutor turns metered as `stage="tutor"` rows (~2 calls, ~$0.0006/turn). **Two more bugs found live & fixed:** (1) `record_tutor_turn` used `asyncio.run()` → RuntimeError inside the `/chat` event loop; added `record_tutor_turn_async` (both chat handlers now use it), sync variant retained for worker/tests. (2) tutor `astream` logged every chunk's `usage_metadata` (multi-row, `input_tokens` 0) → now collapses to ONE row from max tokens seen across the stream. Embedding usage came back 0 because the embed API returns `metadata: None` → `GeminiEmbeddingFunction` now falls back to chars of the formatted text actually sent.

- **P6.3 live pipeline test (2026-08-13) — first real YouTube run since the feature shipped.** Processed `https://youtu.be/tEORX_PevRM` ("AI Agents Are Not Complicated…", 13.6 min) end-to-end via the UI (guest auth, ~8 min wall time): download → transcription (378 segments) → chunking (24 chunks) → extraction (24/24 knowledge objects; one 503 auto-retried) → visual (4 chapter batches) → outline (4 chapters) → merge → chapters → artifacts → tutor/screenshot indexes → cleanup. **Two P6.3 pipeline bugs found & fixed (both would have left seek maps sparse):** (1) `VisualObjectItem` (`visual/visual_extractor.py`) had no `start`/`end`, so LLM-produced visual objects saved without timestamps (only `create_empty_visual_object` fallbacks carried them → only 4/24 chunks had times); now the model carries `start`/`end` and the batch save stamps them from the chunk mapping. (2) `merge_objects` read `visual_object.get("start")` with no fallback — added a fallback to `knowledge_object` start/end so merged objects always carry times (also let the lecture be backfilled with a deterministic zero-LLM re-merge). **Result:** `/video-map` now returns all 4 chapters (0→814.72s, contiguous) + all 24 chunk times. **Live browser verification:** floating "Watch video" button (bottom-right) opens the docked YouTube embed via IFrame API (`/embed/tEORX_PevRM`); sidebar chapter rows + NotesView chapter headings carry seek chips with correct times (0:00 / 3:34 / 7:00 / 10:24); clicking "Play chapter 3" auto-opened the dock and delivered the seek straight to the live player (store `pendingSeek` null, `player` registered); `videoId: tEORX_PevRM`, `embeddable: true`, zero app console errors. Full offline suite green (`scripts/run-tests.sh` 34/34 incl. `visual/test_visual_cache.py`).

- **P6.3 click-to-video grounding (ROADMAP.md P6.3 — shipped; DoD closed at chapter-level best-effort).** Video UI: a floating **"Watch video"** button (bottom-right of the DocPanel) reveals a docked YouTube player on click; any seek link (sidebar chapter rows, NotesView chapter headers, tutor ReferencesPanel rows) sets `open: true`, so the player auto-reveals and seeks. The player streams straight from YouTube — the local video copy stays transient (already deleted post-run by `orchestrator.py` `_TEMP_DIRS`), so no storage policy change was needed. **Backend:** `backend/video_map.py` builds a per-lecture seek map from `merged_objects/chunk_*.json` (`start`/`end` sec) + `notes/lecture_outline.json` (`chunk_ids`); new `GET /video-map?lecture_id=`; `/lectures/{id}` now merges DB `source_type`/`source_url`/`status`/`duration_seconds`. **Frontend:** `useVideoStore` owns source info + seek map + the player handle + an `open` flag — any component can request a seek, buffered until the player is ready; `VideoPlayer` renders nothing unless embeddable (youtube + completed + videoId), lazy-loads the IFrame API on open, creates/destroys the `YT.Player`, and shows an onError banner for private/removed videos (non-embeddable lectures render nothing). **Pipeline fix (found while smoke-testing):** `KnowledgeObject` never persisted chunk `start`/`end`, so `merge_objects_without_visual` always wrote 0 — added the fields in `extract/models.py` + `extract/extractor.py`; new/refreshed runs now carry timestamps (existing cached lectures aren't backfilled — `.extract.sha256` keys off inputs only, graceful). **Vite proxy fix:** `/video-map` was missing from the allowlist → apiFetch got SPA HTML; added. **Verify:** `backend/test_video_map.py` + full `scripts/run-tests.sh` 34/34; frontend 67/67 Vitest (new `video.test.ts`, `useVideoStore.test.ts`, `VideoPlayer.test.tsx` incl. floating-button/open behavior), `tsc -b && vite build`, oxlint clean. Live (2026-08-13, `bf7e8248-…`): fresh load → no bar/iframe, only the bottom-right button; click opens the embed; close destroys it; ch-3 seek auto-opens the player; legacy `1c4400c1-…` (no row) renders nothing. Note: `/video-map` for existing pre-timestamp lectures returns empty maps → seek links stay inactive.

- **P6.2 flashcards QA pass + 2 frontend fixes (FlashcardsPanel).** Live browser QA of the Cards panel (SM-2 scheduling + Anki export, lecture `1c4400c1-…`, chapter 1, 3 real cards): render, flip + hint, all four ratings, footer stats, `Export Anki` (valid 61 KB `.apkg`), and the Due/Missed filters exercised end-to-end against the real `/flashcards/ratings` POSTs. **Fix 1 — filtered-deck badge showed the wrong card's schedule:** the front-card badge (`isCardDue`/`dueLabel`) indexed `cardKeys[current]`, but `current` walks the *filtered* `cards` while `cardKeys` is parallel to the *unfiltered* `allCards` — in Due/Missed decks the badge displayed a different card's due state (e.g. an unrated default-gateway card showed the rated switch card's `IN 1D`). Now resolved via the displayed card's own key: `card ? cardKeys[allCards.indexOf(card)] : undefined`. **Fix 2 — blank card after rating empties/shrinks a filtered deck:** the post-rating auto-advance used a stale `total`/`current` closure, so rating a card that leaves the deck (e.g. `Again`/`Good` → not-due) could push `current` past the new deck length and render an empty card (observed "Card 2 of 1", blank front). Added a `useLayoutEffect` that clamps `current` to `cards.length - 1` when out of bounds (before paint). Verified: Due deck correctly excludes rated cards and shows remaining cards with their own badges; rating the last missed card now lands on the empty state ("No missed cards found!" + Show All Cards) instead of a blank card. `tsc -b && vite build` + `oxlint` + `npm run test` (42/42) green.

- **P6.1 live-verified — real token streaming now actually streams (ROADMAP.md P6.1).** Live Gemini turn (lecture `1c4400c1-…`, one paid turn) exposed that the offline-verified implementation was re-emitting the whole answer: `generate_answer_node` called `llm.ainvoke(...)`, so `astream_events(version="v2")` saw the finished answer as ONE `on_chat_model_stream` chunk — and the node filter compared `metadata["langgraph_node"]` against `"generate_answer_node"` while the graph registers the node as `"generate_answer"`, so every streamed event was dropped and the no-stream fallback re-emitted the cached whole answer. Fixes: (1) `tutor/nodes.py` `generate_answer_node` now iterates `llm.astream(...)` and accumulates chunk text (commit-state answer unchanged); (2) `backend/dependencies.py` filters on `"generate_answer"`. Live result: **11 incremental token frames, TTFT 9.74s** (that number includes dense+BM25 retrieval, query rewrite and image retrieval — the subsequent tokens arrive continuously); `{final}` carries verified citations + assistant_message_id. `tutor/test_async_persistence.py` `FakeLLM` gained `astream` for the node contract change; `scripts/run-tests.sh` 31/31. Full pipeline run on a lecture is NOT required for streaming — retrieval only needs the index.

- **P6.1 real token streaming (ROADMAP.md P6.1 — code complete, verified offline).** `/chat/stream` no longer computes the full answer then re-chunks it in fake 24-char frames: new `astream_tutor_tokens()` (`backend/dependencies.py`) drives the graph with `astream_events(version="v2")` and yields only `on_chat_model_stream` events whose `metadata["langgraph_node"] == "generate_answer"` — summarizer / query-rewrite / quiz-evaluate LLM calls never leak into the visible stream (rewrite tokens would otherwise pollute the answer). Extracted reused dedupe logic into `_cached_turn()` (identity-scan dedup, `_content_text` for Gemini thinking-model content-block lists) and added `_chunk_text()` for chunk payloads; the streaming path holds the same per-lecture lock and zombie/dedupe guards as `ainvoke_tutor`, so `/chat` and `/chat/stream` can't interleave a thread. Two behaviors preserved on purpose: a dedupe hit streams the cached answer as a single `t` frame + `final` (no re-invocation), and quiz/command turns that answer without an LLM stream emit the whole answer once (fallback when `streamed_any` is false). `tutor/llm.py` `UsageLoggingChatLLM` gained an `astream` override so streamed calls still log aggregate token usage (read from the last chunk's `usage_metadata`) to `outputs/llm_calls.jsonl`. SSE wire contract unchanged (`data: {t}` / `data: {final}` / `data: [DONE]` / `data: [ERROR]`) — `frontend` untouched. New `tutor/test_tutor_streaming.py` (5 offline checks, no API): real token reassembly via `GenericFakeChatModel` (strings join back to the stub answer), node-filtering (rewrite text absent), no-stream fallback (whole answer as one frame), dedupe (identical re-ask makes zero new LLM calls), zombie-thread guard (raises instead of streaming). Offline suite green (`test_chunker`, `test_async_persistence`, `test_tutor_streaming`, `test_api_contract_offline`, tutor graph/memory/chapter/low-confidence/test_schema suite); `test_api_contract.py` has a single pre-existing unrelated `/study-guide` 404 (confirmed via stash). `/chat/stream` TTFT drops from "full answer wait" to first real token.

- **P5.8 frontend tests + API contract tests (ROADMAP.md P5.8 — code complete, verified).** CI already ran `npm run test` but no script existed — that job was failing on every push; now fixed. **(Frontend stack)** Vitest 4 + React Testing Library + jest-dom + jsdom added (the RTL/jest-dom installs needed `@testing-library/dom` as a separate devDep — v7 doesn't pull it). `test` script = `vitest run`; new `vitest.config.ts` (jsdom, setup file, includes `src/**/*.test.{ts,tsx}`); `src/test/setup.ts` registers jest-dom matchers + RTL cleanup. Tests live under `src/` so the app's own `tsc -b` (strict) typechecks them; explicit vitest imports (no globals) keep `types: ["vite/client"]` untouched. **38 tests across 7 files:** unit — `parseChapter` (structured/JSON-string/guide-`{markdown}`/markdown/null payloads), `references` (confidence gate incl. `relevant` overrides, verified-citation precedence + unverified drop, rawText Sources fallback only-when-retrieval-empty, screenshot appends), `http` (ApiError detail extraction, non-JSON success rejection, `apiFetch` null-on-failure, AbortSignal pass-through), `conceptMapLayout` (determinism, root centering, radial bounds); components — `Markdown` (headings/strong/GFM table/KaTeX), `AppErrorBoundary` (healthy children + recovery panel on render error), `useQuizStore` `question_id`→`id` normalization with mocked `fetch`. **(Backend contract)** new `backend/test_api_contract_offline.py` boots the app in-process via `TestClient` with lifespan startup (Alembic migrations on a temp SQLite DB, pipeline supervisor daemon, GC sweep) — 15 checks, fully offline: `/docs` html, `/openapi.json` exposes `/process`/`/chat`/`/quota`, `/lectures`+`/outline`+`/quiz/questions` shapes, anonymous `/quota` trial shape (15 min, `is_anonymous`), `/process`+`/billing` 401 unauthenticated, `/process` validation negatives (unsupported `source_type`, bad youtube/gdrive URLs, upload w/o file), `/chat` empty body 422. `scripts/run-tests.sh` picks it up automatically (30/30 offline suite green) while the live-server probe `backend/test_api_contract.py` stays excluded. Full matrix verified: `npm run test` 38/38, oxlint (only the 8 pre-existing warnings), `tsc -b && vite build` green, `scripts/run-tests.sh` 30/30. Test-side fixes during the pass: two assertions corrected to real behavior (`headingToId` yields `sec-`-prefixed ids; the plain-markdown parse path preserves image markdown — only structured payloads strip it) and the http error test mocked fresh `Response`s (a reused mock Response body is consumed once).

- **P5.4 Docker + deploy config (ROADMAP.md P5.4 — built + smoke-verified on Docker Desktop 4.83 WSL2).** Single-container multi-stage image (`Dockerfile`: node:20-alpine → `npm ci && npm run build`, then python:3.12-slim → `pip install -r requirements.txt` + ffmpeg → `COPY` repo + `frontend/dist`; `CMD uvicorn backend.main:app --host 0.0.0.0 --port 8000`, `NORAI_SPA_DIST=/app/frontend/dist`). Compose (`docker-compose.yml`): `build: .`, repo-root `.env` via `env_file`, `8000:8000`, named `norai_outputs` volume on `/app/outputs`. **Build blockers found & fixed (two failed boots):** (1) `npm ci` ERESOLVE — the lockfile only ever installed under legacy peer resolution (openapi-typescript 7 peer-requires `typescript@^5`, repo pins `~6.0.2`); added `frontend/.npmrc` (`legacy-peer-deps=true`) so host + container behave identically; the `Dockerfile` frontend stage now also `COPY`s `.npmrc`. (2) `ModuleNotFoundError: gdown` (ingest), then `langgraph.checkpoint.sqlite` (tutor checkpointer) — both are direct imports only present ad-hoc/transitively in the dev venv; `requirements.txt` gained six exact pins: `gdown==6.1.0`, `imagehash==4.3.2`, `langgraph==1.2.4`, `langgraph-checkpoint-sqlite==3.1.0`, `numpy==2.4.6`, `pillow==12.2.0`. **Verified:** container stays up; `/docs` + `/openapi.json` 200; SPA served for `/` (`<title>NorAI — Your AI Tutor</title>`) and client routes `/app`, `/workspace/…`, `/print?…` when `Accept: text/html` (API JSON routes unaffected — the middleware checks `text/html` exactly like the Vite dev proxy bypass); `docker compose config` valid. Missing dep detection used `ast`-walked top-level imports × `importlib.metadata` distribution mapping — no other used-but-unpinned distributions remain.

- **P5.6 frontend bundle & resilience (ROADMAP.md P5.6 — code complete, verified).** **(Bundle)** Route-level code-splitting in `App.tsx`: all 7 pages (`LandingPage`/`PricingPage`/`BillingPage`/`UploadPage`/`ProcessingPage`/`Workspace`/`PrintPage`) are now `React.lazy` + a themed `<Suspense>` fallback. Entry chunk **1,353.71 kB → 363.78 kB** (400.85 → 117.16 kB gzip); per-page chunks: Landing 19.4 / Pricing 6.0 / Upload 12.0 / Processing 5.4 / Billing 7.9 / Print 12.0 / Workspace 95.8 kB; auth 219.4; the 613 kB shared chunk (`useQuizStore`) holds the markdown/KaTeX/highlight stack and loads only when the workspace loads. **(Shared renderer)** New `src/components/ui/Markdown.tsx` — one `Markdown` component (`variant: doc|revision|print|chat`) over the existing `createMarkdownComponents` set; the 5 duplicated `ReactMarkdown`+plugins sites (`MessageBubble`, `NotesView`, `RevisionView`, `StudyGuideView`, `PrintChapter`) migrated to it. `import 'katex/dist/katex.min.css'` moved out of the global `index.css` into that module, so KaTeX fonts/styles ship only in workspace/print chunks — marketing routes no longer download them (verified: landing network log has zero katex/highlight/markdown requests). **(Resilience)** New app-level `AppErrorBoundary` (was print-route-only) with a themed recovery panel + reload. **(Chat)** `MessageBubble`, `ReferencesPanel`, `ShimmerLoader` wrapped in `React.memo` so per-stream-token re-renders skip re-parsing completed bubbles. Granular Zustand-selector rewrite deferred (React Profiler-driven follow-up). Browser smoke: landing (lean), workspace Notes + Revision render from real lecture artifact, print route renders all chapters — no console errors, no paid pipeline runs. `npm run build` (`strict: true` + vite) + `oxlint` green.

- **P5.5 frontend typed API client (ROADMAP.md P5.5 — code complete).** New `frontend/src/lib/http.ts`: single typed wrapper (`apiGet`/`apiPost`/`apiDelete`/`apiFetch`/`apiFetchRaw`) that merges auth headers, passes `AbortSignal` through, and normalizes errors into a typed `ApiError` (extracts `detail`; non-JSON responses rejected). Response types are explicit generic `T` at the call site, or derived from the openapi-typescript `paths` schema (`ApiJson<P, M>`) when `T` is omitted — so regenerating `frontend/src/types/api.ts` from FastAPI's OpenAPI tightens callers automatically as the backend declares `response_model`. A single shared `API_BASE` lives in `http.ts` (was duplicated in `chatApi.ts` + `useQuizStore.ts`, now imported). All raw `fetch`+`authHeaders` call sites migrated (UploadPage estimate/lectures/process, ProcessingPage poller, BillingPage, print data loader, doc views, stores). Every `any` removed from `frontend/src` — print pipeline typed (`PrintChapterPayload`, `parseChapterContent` type guards incl. guide `{markdown}` unwrap), catch blocks narrowed to `unknown`, `ChatArea` screenshot ref typed as `Reference`, quiz question normalization typed. Enabled `strict: true` in `tsconfig.app.json` — full `tsc -b` clean with forced rebuild; `npm run build` + `oxlint` green. (P5.4 Docker files drafted separately: uncommitted `Dockerfile`/`docker-compose.yml`/`.dockerignore`, single-container multi-stage image, not yet verified — now built & verified, see the P5.4 entry above.)

- **P5.1–P5.3 foundation (ROADMAP.md Phase P5, part 1 — code complete).** **(P5.1 observability)** new `backend/logging_config.py`: `setup_logging()` writes structured JSON (console + rotating `outputs/backend.log`), `bind`/`get_context`/`clear_context` wrap a contextvars store keyed by `request_id`/`lecture_id`/`thread_id`. `backend/main.py` gains a request-id middleware (accepts/echoes `X-Request-ID`); `backend/jobs.py` binds `lecture_id` per job run; `/chat` + `/chat/stream` bind `lecture_id`/`thread_id`. New `tutor/llm.py`: `make_chat_llm(node=...)` returns a `UsageLoggingChatLLM` that appends per-call Gemini token usage to `outputs/llm_calls.jsonl` (thread-safe, defensive no-op on failure); every tutor LLM site now imports it lazily (`nodes.py`, `nodes_retrieval.py`, `quiz_nodes.py`, `backend/main.py:/quiz/evaluate`). **(P5.2 deps)** `requirements.txt` exact-pinned + trimmed to what's imported (`google-genai==2.8.0`, `langchain-google-genai==4.2.5`, `langchain-core==1.4.6`, `faster-whisper`, `chromadb`, fastapi/uvicorn/sqlalchemy/aiosqlite/asyncpg/alembic/pyjwt…); new `requirements-dev.txt`; `.env.example` committed. **(P5.3 CI)** `.github/workflows/ci.yml` (backend: `scripts/run-tests.sh` over all offline `test_*.py`; frontend: `npm ci` + oxlint + `tsc -b`/`vite build` + `npm run test`) + `scripts/run-tests.sh`. **Offline-test fix:** `tutor/nodes.py` bound `make_chat_llm` at module import, so tests that patch `tutor.llm.make_chat_llm` silently hit the real Gemini API; moved to lazy in-function imports (matching `nodes_retrieval.py`/`quiz_nodes.py`) — verified all 67 offline tests pass with a garbage key. Renamed `assessment/test_schema.py` → `assessment/schema_probe.py` (real-API probe, excluded from CI); deleted dead `embeddings/embed.py`.

- **P4.4–P4.5 tutor async persistence + progress transport (ROADMAP.md P4.4/P4.5 — code complete, all offline; P4 DoD validated offline).** **(P4.4)** Tutor turns no longer block the event loop: `tutor/memory.py` gains `get_async_checkpointer` (`AsyncSqliteSaver`, WAL + busy_timeout); the LLM-touching graph nodes are now `async` (`generate_answer_node` + `save_memory_node` in `nodes.py`, `rewrite_query_node` in `nodes_retrieval.py`, `quiz_llm_evaluate` in `quiz_nodes.py`); `backend/dependencies.py` rewritten around `ainvoke_tutor` — per-lecture `asyncio.Lock`, LRU graph cache bounded at `config.TUTOR_MAX_CACHED_GRAPHS` (32, connections closed on eviction), zombie-thread guard. `/chat`, `/chat/stream`, `/threads/{id}` all `await` the async path; `tutor/cli.py` async too. Verified by `tutor/test_async_persistence.py` (cross-turn persistence + thread isolation, LRU eviction closes conns, per-lecture serialization) + updated `test_memory_nodes.py`; all 11 offline tutor tests green. **(P4.5)** Decision: real SSE dropped — the dead `_queues`/`run_coroutine_threadsafe` scaffolding was removed from `orchestrator.py`; progress stays on the DB-backed `/process/{id}/status` poll. `ProcessingPage.tsx` poller hardened: AbortController on unmount, exponential backoff 1.5s→10s (reset on success), 404 → terminal error, `finished` dropped from the effect deps (killed the latent extra-request on the finish flip). oxlint + `tsc -b` green. **P4 DoD (restart survival) validated offline**: new `backend/test_jobs_restart.py` (19 checks) simulates a crash mid-run against a temp SQLite DB with a stubbed pipeline — a stale `processing` job is recovered → re-queued (attempts+1) → re-claimed → resumes to `completed` with progress persisted; exhausted attempts → `failed`; fresh-heartbeat jobs are left untouched; mid-run failures retry while attempts remain.

- **P4.1–P4.3 job durability, GC + Alembic migrations (ROADMAP.md Phase P4 — code complete, offline-verified).** **(P4.1)** DB-backed job queue replaces unbounded daemon threads + in-memory task store: new `backend/jobs.py` — a supervisor thread (started idempotently at app startup) polls the DB, recovers `processing` jobs with stale heartbeats (re-queue ≤ `PIPELINE_MAX_ATTEMPTS`(3) or fail), claims `queued` jobs within global (`MAX_CONCURRENT_PIPELINES`) + per-user (`MAX_PER_USER_PIPELINES`) caps, and runs them in a bounded `ThreadPoolExecutor`. Lifecycle `queued → processing → completed | queued (retry) | cancelled | failed`. Worker threads persist `stage`/`stage_message`/`progress` + refresh `heartbeat_at`; an in-memory cancel flag (persisted `cancel_requested`) is checked between pipeline stages → `PipelineCancelled`. Because pipeline stages are cache-first, recovering a stuck job is a near-free resume. `POST /process` now enqueues (`status="queued"` + `queued_at`) and no longer spawns a raw thread or owns upload cleanup (worker does); `GET /process/{id}/status` is DB-backed and maps the lifecycle onto the legacy `{stage,message,progress}` poll contract (`completed→complete`, `failed/cancelled→error`, else live stage) so `ProcessingPage.tsx` is unchanged; new `POST /process/{id}/cancel` → `request_cancel`. **(P4.2)** cleanup on failure/restart: worker deletes the upload after the run (success/failure/cancel); `jobs.gc_sweep()` runs at boot (+ daily in the supervisor) purging stale `outputs/uploads` files and DB-orphaned lecture dirs; orchestrator already removes transient dirs on every outcome. **(P4.3)** Alembic versioned schema: new `alembic.ini`, `backend/db/migrate.py` (`run_migrations` on startup), `migrations/versions/0001_initial` + `0002_lecture_pipeline_job_columns` (adds `stage`, `stage_message`, `progress`, `attempts`, `heartbeat_at`, `queued_at`, `started_at`, `cancel_requested`), legacy `create_all` absorption + backfill, `requirements.txt` +`alembic`. New config: `backend/db/models.py` queue columns; `config.py` queue constants. New test: `backend/test_migrations.py` (13 checks: fresh + legacy + idempotent). Verified offline: migrations pass, all-queue smoke test (enqueue → claim → stale-recovery → cancel → progress persistence) green, `py_compile` OK, all routes registered. **Not done: P4.4 (async tutor persistence) and P4.5 (SSE) remain open.** Note: supervisor assumes a single uvicorn worker (documented in `jobs.py`; needs a DB claim-lock if `--workers > 1`).

- **Phase P3 retrieval/tutor hardening (ROADMAP.md P3.1–P3.8 — code complete, all offline).** **(P3.1)** golden-QA eval suite: `tutor/evals/` — per-lecture `golden_sets.py`, MRR/hit-rate `runner.py`, calibration + regression `test_evals.py` (threshold sweep covered). **(P3.2)** hybrid search: `tutor/bm25.py` (rank-bm25, lazy build) + `retriever.py` RRF fusion (dense cosine + BM25, 60/40), chunks now carry `chunk_id`/`relevant`/`distance`; `test_hybrid.py`. **(P3.3)** verified citations: `tutor/citations.py` deterministically post-checks the answer's Sources against retrieved chunk IDs — no LLM; `verify_citations_node` added to the graph (`generate_answer → verify_citations → save_memory`); backend surfaces `verified_citations` in stream + thread-snapshot responses; frontend `VerifiedCitation` type + `buildReferences` renders only verified citations; `test_citations.py`. **(P3.4)** context expansion: `tutor/context_expand.py` pulls parent-section + sibling chunks around leaf hits; `test_context_expand.py`. **(P3.5)** cross-turn chapter state: `detect_chapter_node` writes `last_chapter_id`; anaphoric follow-ups ("what about that?", "why is that?") re-scope retrieval to the remembered chapter; fresh questions go full-index; rewrite prompt sees the active chapter; `test_chapter_tracking.py`. **(P3.6)** memory hardening: `save_memory_node` now returns `RemoveMessage`s for summarised turns (checkpoint store stays bounded, not just the prompt window) + `_recent_window` char-budget (8000) bounds the retained window; `test_memory_nodes.py` updated for the new contract. **(P3.7)** graceful low-context: `retrieve_node` sets `retrieval_status` ("ok"/"empty"/"error") and tags chunks `confidence_tag` strong/weak vs `CONFIDENCE_THRESHOLD`; `prompts.py` gains `_RETRIEVAL_ERROR_NOTE` + `build_context_block(status=...)`; `test_low_confidence.py`. **(P3.8)** confidence-gated references already live in `frontend/src/lib/references.ts` (`isConfident`, rawText fallback only when nothing retrieved) + TS types. All Python offline tests green (evals/hybrid/citations/context_expand/chapter_tracking/memory/low_confidence/graph_topology), `tsc -b && vite build` + `oxlint` green. No paid pipeline runs. Note: hybrid BM25 is implemented in-repo (`tutor/bm25.py`, no new dependency — a drop-in `from rank_bm25 import BM25Okapi` swap is documented for later if ever wanted).

**DoD VALIDATED** (2026-08-08, ~8-min YouTube lecture): first run = **17 Gemini calls** (6 extraction + 1 outline + 3 visual + 2 screenshot Pass-2 + 3 notes artifacts + 2 embed batches); re-run of the same lecture id = **0 Gemini calls** (all paid stages cache-hit — `Knowledge extraction: up to date`, `Outline generation: up to date`, `Visual extraction: up to date`, `Chapter N screenshot selection: up to date`, `Chapter N consolidated artifacts: up to date`; Chroma diff-sync `added 0, removed 0` on both indexes). Fix landed during validation: orchestrator Stage-19 cleanup previously **deleted** `objects/`, `visual_objects/`, `merged_objects/` — silently wiping the extraction + visual-analysis caches every run; those dirs are now preserved, and whole-stage markers were added for visual extraction (`.visual_extract.sha256`) and outline generation (`.outline.sha256`). New tests: `visual/test_visual_cache.py`, `notes/test_outline_cache.py`. (Baseline note: the first YouTube video chosen for the paid run was 25.8 min → correctly blocked by the 15-min free-trial gate; the ~8-min second video was used instead.)

- **P2 billing & quota (ROADMAP.md Phase P2 — code complete, verified live end-to-end).** **(P2.1)** usage metering: new `backend/usage.py` — `record_pipeline_outcome` (success/failure) increments `Subscription.used_minutes_this_month` + writes `UsageLog`; orchestrator success/failure paths both call it; `Lecture.duration_seconds` persisted. **(P2.2)** quota enforcement *before* any spend: `/process` now requires auth (401 without a valid Bearer), checks `used >= quota` (429), then the free-trial duration cap (429), then duration-aware `used + needed > quota` (429) — all before probe/download/Gemini. **(P2.3)** lecture status persisted (`completed`/`failed` + `duration_seconds`) via the same outcome call. **(P2.4)** live quota badge: new `GET /quota` (`main.py`) returns plan/status/quota/used/remaining/`is_anonymous`; Sidebar badge reads it through `useAuthStore.refreshQuota` (`authHeaders()` Bearer). **(P2.5)** billing page: new `GET /billing` (`main.py:1105`) returns plan/usage/subscription + Lemon Squeezy `checkout_urls`/`manage_url` (env-driven, null until store exists); new `frontend/src/pages/BillingPage.tsx` (tier, usage bar, Starter/Pro plan cards, manage-subscription section) + `/billing` route in `App.tsx`. New tests: `backend/test_billing_quota.py` (20 assertions incl. 401/429 gates) + `backend/test_usage.py` (28, metering/reset/rollback). Live fixes caught in QA: (1) Vite `/billing` proxy collided with the SPA route — `vite.config.ts` now `bypass`es text/html navigations to `/index.html` (JSON fetches still proxy to :8000); (2) **infinite refetch loop** — `refreshQuota` rebuilt `user` on every call (new object ref → BillingPage/Sidebar effects re-ran → `/billing`+`/quota` piled up `[pending]`, page stuck on "Loading billing…"); fixed by only emitting a new `user` when `subscriptionTier`/`monthlyQuotaMinutes`/`usedMinutesThisMonth` actually change. Verified in browser: anonymous sign-in → sidebar badge (real 0/15, progressbar `valuemax=15`) → Upgrade → `/billing` renders live data instantly, request count stable. All Python tests + `tsc -b && vite build` + `oxlint` green. Note: real checkout is gated on the Lemon Squeezy store existing (`LEMONSQUEEZY_CHECKOUT_*`/`CUSTOMER_PORTAL_URL` unset → "See pricing" buttons).

- **P1 pipeline economics (ROADMAP.md Phase P1 — code complete, DoD validated).** **P1.1** chunk-size drift fixed: `chunking/chunk.py` now imports `config.DEFAULT_SEGMENTS_PER_CHUNK` (15) instead of a hardcoded 5 (3× fewer extraction LLM calls). **P1.2** batched indexing: new `tutor/build_index.py:upsert_batched()` (batch 20 + `EMBED_BATCH_SLEEP_SEC` pacing); orchestrator Stage 17/18 now delegate to it with content-hashed ids + diff-sync against the per-lecture `tutor/chroma` (identical re-runs are no-ops, orphans deleted — no more one-embed-call-per-doc). **P1.3** duplicate multimodal analysis removed: `visual_extractor` persists `visual_analysis_ch{id}.json` per chapter; `screenshot_selector` loads it (`load_visual_analysis`) and synthesizes Pass 1 `FrameQualityScore`s from `importance_score`/`visual_type`/`include_in_notes` instead of re-uploading + re-scoring the same keyframes. **P1.4** hash-of-inputs caching: new `cache_util.py` (`digest`/`outputs_current`/`write_marker`, `.sha256` marker sidecars) wired into extraction (whole-stage + per-chunk skip), per-chapter note artifacts, and per-chapter screenshot selection — re-runs with unchanged inputs cost ~0 API calls. **P1.5** dead/duplicated code deleted: `retrieval/`, `vectordb/`, `notes/chapter_structurer.py`, `notes/chapter_clusterer.py`, assessment/revision generator + pdf_builder + renderer + parser modules; `revision_generator.py` trimmed to `render_revision_markdown`; `extract/__init__.py` now re-exports from `extractor` (was a stale dup w/ `gemini-2.5-flash-lite`); flashcard langchain scaffolding removed; `from random import random` shadow bug fixed. **P1.6** config constants honored: `DEFAULT_RPM_LIMIT`/`DEFAULT_MAX_RETRIES` now flow into `ratelimit.py`, extraction retries, notes/visual/screenshot retries; `outline_generator.py` uses the shared limiter. **P1.7** cloud transcription upgrade: migrated primary transcription to **Google AI Studio Gemini API (`gemini-3.1-flash-lite`)** with 18-minute parallel audio chunking (`NORAI_AUDIO_CHUNK_MINUTES=18`), yielding **9.2× speedup** (53s for a 71m lecture vs 498s CPU) and 0% host CPU load (eliminating the single-vCPU VPS bottleneck); local `faster-whisper` (`small` model) retained as offline fallback via `NORAI_TRANSCRIPTION_BACKEND="whisper"`. **P1.8** adaptive chunking + self-calibrating pre-flight estimate (LIVE): `adaptive_segments_per_chunk()` bounds extraction chunks to `TARGET_MAX_CHUNKS`(24)/`MAX_SEGMENTS_PER_CHUNK`(60); new `POST /estimate` + `ingest.probe_video_metadata` (yt-dlp metadata-only, ~2s) surface ~calls/~minutes + free-trial fit before processing; UploadPage shows the estimate inline. Every real run now appends actuals to `outputs/pipeline_metrics.jsonl` (LLM/embed counters + stage timings via `backend/estimator.py`); ≥5 fresh runs re-fit segs-per-min, transcription realtime, sec/call, embed-batch time, and Pass-2 selection ratio so the estimate tightens automatically (`python -m backend.estimator --recalibrate`). New standalone tests: `chunking/test_adaptive_chunking.py`, `backend/test_estimate.py` (34 checks). Full suite green (all backend tests, tutor tests, adaptive/estimator), `oxlint` 0 errors, `tsc -b && vite build` green. **DoD validated (2026-08-08)**: consented ~8-min video first-run Gemini calls = **17**, re-run same lecture = **0**; estimator baseline predicts **17** vs 17 observed.

**Performance follow-up (2026-08-12)**: an 8-min video showed a ~14-min UI estimate, which traced to the uncalibrated estimator (not wall-clock) plus a slower whisper `small` transcription and all LLM stages serialized through ONE global 12-RPM limiter (thread counts were never reduced). Measured baseline (same 8:26 video, standalone run): transcription 104.4s, pipeline 399.6s (6.7 min), 19 LLM calls. Fix: whisper `vad_filter=True` + `beam_size=1` (`transcription/transcribe.py`, env knobs `NORAI_WHISPER_VAD_FILTER`/`NORAI_WHISPER_BEAM_SIZE`); RPM now env-overridable via `NORAI_RPM_LIMIT` (`backend/ratelimit.py`). After fix: transcription **60.7s (−42%)**, pipeline **207.2s (3.5 min, −48%)**, **15 LLM calls (−21%)** — transcript quality preserved (88/89 segments). `test_transcribe_config.py` extended to cover the new knobs; `python -m backend.estimator --recalibrate` now fits from 2 fresh runs (transcription_realtime 0.16 vs default 1.0). See `NOTES.md` §1 row 17.

- **P0 security hardening (ROADMAP.md Phase P0 complete).** **(a)** Webhook fail-closed: `verify_signature` rejects when `LEMONSQUEEZY_WEBHOOK_SECRET` is unset (503; dev escape hatch `NORAI_ALLOW_UNSIGNED_WEBHOOKS=1`); webhook now requires explicit `custom_data.user_id`, normalizes `status`, and maps tier from an exact `PLAN_TIER_BY_VARIANT` table (no more `"pro" in variant` substring — spoofable); stable SHA-256 idempotency fallback replaces process-randomized `hash()`. **(b)** `/static` blanket mount replaced with an image-only allowlist catch-all (`resolve_static_path`): transcripts, answer keys, `checkpoints.sqlite`, Chroma DBs, and `backend.log` are no longer servable; traversal blocked; verified via browser image load + curl 404s. **(c)** `/process` input gate: `source_type` validated, YouTube/Drive URLs must pass `is_youtube_url`/`is_gdrive_url` (kills SSRF before any download), upload extension whitelist (415), chunked stream-write with `NORAI_MAX_UPLOAD_BYTES` cap (413), upload file auto-deleted after the pipeline finishes. **(d)** Exception detail leaks closed: `/chat`, `/chat/stream`, `/quiz/evaluate` return safe messages (raw detail → `logger.exception`), plus a global `@app.exception_handler(Exception)` → generic 500. **(e)** Auth: duplicate-email race now resolves instead of silently demoting to anonymous (`get_or_create_user_from_token` IntegrityError → rollback → re-fetch; missing Subscription backfilled); `is_anonymous` now trusts the Supabase `is_anonymous` JWT claim, not the forgeable `@anonymous.norai` email suffix. **(f)** Ownership scoping (`ensure_lecture_access`): 12 artifact-read endpoints 404 on foreign lectures **when a token is presented**; anonymous + legacy `default` unchanged (dormant today — the frontend sends no token on reads; deliberately not wired to avoid 404ing pre-auth lectures). New standalone tests (48 assertions): `backend/test_webhooks.py`, `backend/test_auth_email.py`, `backend/test_upload_validation.py`, `backend/test_static_allowlist.py`; `test_api_contract.py` green; `npm run build` + `oxlint` green; no paid pipeline runs.
- **Supabase wired end-to-end (Postgres + Auth).** Backend DB now points at Supabase Postgres via `DATABASE_URL` (Session pooler, `aws-0-ap-southeast-2.pooler.supabase.com`; the direct `db.<ref>` host is IPv6-only and unreachable on this machine). `init_db()` created the 5 SaaS tables (`users`, `subscriptions`, `lectures`, `usage_logs`, `webhook_events`) in the public schema. `backend/auth.py` hardened: JWT verification now handles **ES256** (this project's signing scheme) via the Supabase JWKS endpoint (cached `PyJWKClient`, keyed by `kid`) and falls back to HS256 via `SUPABASE_JWT_SECRET` for legacy; `aud` must be `authenticated`/`anon`; fails closed (dev escape hatch only under `NORAI_DEV_INSECURE_AUTH=1`). `database.py`/`auth.py` now call `load_dotenv()` themselves so they work when imported standalone. Frontend: added `@supabase/supabase-js`, new `lib/supabaseClient.ts` (reads `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`), `useAuthStore.ts` rewritten to derive state from `supabase.auth` (`onAuthStateChange`, token refresh, session persistence in Supabase's own storage — reload-safe), `AuthModal.tsx` now does real `signUp`/`signInWithPassword`/`signInWithOAuth`/`signInAnonymously` (email signup shows a confirm-inbox notice; errors render in a banner). `vite.config.ts` gained `envDir: '..'` (Vite now reads the repo-root `.env`, where the `VITE_*` vars live) and a `/quota` proxy entry. `chatApi.ts` + `UploadPage.tsx` attach `Authorization: Bearer <access_token>` via new `lib/authHeaders.ts`, so `/quota` and `/process` authenticate and create real `Lecture` rows. Verified end-to-end: anonymous guest sign-in → real Supabase session persists across reload → backend verifies ES256 JWT via JWKS → `User` + trial `Subscription` created in Supabase Postgres → `/quota` returns the real per-user quota through the Vite proxy. `npm run build` + `oxlint` + `backend/test_api_contract.py` green. Note: Google OAuth button is wired (`signInWithOAuth`) but the provider isn't enabled in the dashboard yet.
- **PDF exports: proper Guide + Mind map PDFs, interactive HTML export, continuous-flow Revision/Guide.** (a) Guide PDF button no longer maps to the revision deck — new `guide` print type loads `GET /study-guide` (title cover page + real chapter titles). (b) Mind map PDF button no longer maps to revision — new `concepts` print type renders each chapter's concept map as a static **vector SVG** (`PrintConceptMap.tsx`) using the shared layout engine; all chapters, one map per page. (c) Shared `frontend/src/lib/conceptMapLayout.ts` extracted from `ConceptMapView.tsx` (tree/radial positions + bezier edge path) so the on-screen map, print SVG, and interactive export stay in sync. (d) New `exportInteractiveMindmap.ts` + "Interactive" button on the Mind map tab downloads a **self-contained HTML** (inline CSS + vanilla JS: pan/zoom, clickable nodes with description cards, per-chapter tabs) — the interactive counterpart to the static PDF. (e) Print node boxes now cap width and ellipsize long labels (full text as SVG tooltip) to stop wide chapter titles colliding with the focus column — verified 0 collisions across all 9 chapters of a real lecture. (f) **Continuous flow**: `Revision` and `Guide` PDFs no longer force one chapter per page — new `.print-flow` print CSS (`break-before: auto`, `break-after: avoid` on headings, thin divider between chapters); `PrintChapter` gained a `continuous` prop. Notes (with screenshots) and Assessment keep page-per-chapter. Verified in browser: revision/guide = 9 `.print-flow` + guide cover; notes/assessment unchanged. `tsc -b && vite build` + `oxlint` green; no paid pipeline runs (all data from existing artifacts).
- **Landing/Pricing page review pass (frontend-design + Web Interface Guidelines + MiMo screenshot QA).** Fixed a critical typography bug: `LandingPage.tsx`/`PricingPage.tsx` used numeric `text-*` utilities (`text-12/15/17/18/20/22/28/36/44`, plus invalid `*-bold`/`*-regular`/`*-medium` combos) that were **not defined in `@theme`** — verified absent from compiled CSS, so the entire type scale silently collapsed to 16px. Added `--text-10…44` tokens to `index.css` `@theme` and rewrote the `*-bold` classes as `text-<size> font-bold`. A11y/compliance: skip link (`App.tsx` → `id="main"` on all page roots), `scroll-margin-top` for `#features`/`#how-it-works`, Pricing nav `<button>`→`<Link>`, removed dead `#privacy`/`#terms` anchors, mobile nav menu (hamburger + `aria-expanded`), ARIA tabs pattern on the workspace mockup (`role="tablist"/tab/tabpanel` + roving tabindex + arrow/Home/End), `role="switch"`+`aria-checked` billing toggle, `tabular-nums` on prices, `text-balance` on headings, reduced-motion respected via global `<MotionConfig reducedMotion="user">` (framer animations previously ignored `prefers-reduced-motion`), `transition-all`→explicit properties, decorative grid `aria-hidden`. Copy: deduped pricing h1, tightened free-plan tagline, curly apostrophe. MiMo (opencode/mimo-v2.5-free) screenshot QA surfaced a real mobile overflow: the mockup tabs bar's nowrap min-content (~477px) widened the section past the 390px viewport and got clipped by `overflow-x-hidden`; fixed with `w-full` on the mockup section + `overflow-x-auto` tabs bar + title truncate + mobile-hidden status badge. `npm run build` (tsc) + `oxlint` green; verified tokens/roles present in compiled `dist/`; visual re-QA: all screens PASS.
- **Feature-plan QA review + fix pass.** Full review of the A–F NotebookLM-parity deliverables; the real gaps found were fixed. **(1) Tutor memory** — `context_messages` was never populated, so the LLM prompt was effectively stateless (current question only). `tutor/nodes.py` `load_memory_node` now rebuilds the prompt window each turn from `messages` (last 6 Human/AI + most recent `_SUMMARY_PREFIX` summary); `save_memory_node` summarizes incrementally (turns after the last summary, triggered at 12+ new messages); `get_thread` skips `SystemMessage`s so summary records never leak into the UI. **(2) Missed-question correctness** — `GET /quiz/attempts/{id}/missed` now uses a deterministic `_compute_missed_ids` (MCQ/True-False by string equality, free-text by remark negative-marker detection) instead of the substring search that marked "Incorrect. The correct answer is X" as correct. `finish_quiz_attempt` is lecture-scoped with a `rowcount` 404 and persists optional `correct_ids_json` (lazy-ALTER migration). **(3) Quiz load path** — removed the `AIPanel` chapter-change effect that auto-started an all-difficulty quiz, overriding difficulty-filtered / missed-question / in-progress quizzes; single entry via segmented control + AssessmentView Start Quiz. **(4) Flashcard stats** — Reviewed/Got it/Almost/Left now computed from the current deck's card keys (was chapter-wide). **(5) Cleanup** — removed dead `_get_all_thread_ids` + duplicate imports. New tests: `backend/test_quiz_missed.py`, `tutor/test_memory_nodes.py`. Verified already-correct: retriever per-lecture client caching, `CONFIDENCE_THRESHOLD=0.35`, lecture-switch flush, ConceptMap Ask-Nora dedup. All Python tests + `oxlint` + `tsc -b && vite build` green; no paid pipeline runs.
- **QA fix pass — quiz/flashcard persistence 500s + tutor message duplication.** `backend/main.py` `_db()` took 0 args but every quiz/flashcard caller passed a `lecture_id` → all 6 persistence endpoints (`POST /quiz/attempts`, `/quiz/attempts/{id}/finish`, `GET /quiz/attempts`, `/quiz/attempts/{id}/missed`, `POST /flashcards/ratings`, `GET /flashcards/ratings`) threw `TypeError`/500. `_db(lecture_id="default")` now routes to the per-lecture DB (`get_lecture_db_path`; default keeps root `CHECKPOINT_DB_PATH`). `backend/dependencies.py` dedup now scans the last 3 human messages (was only the last) and returns the AI answer directly following the matched message via identity scan (was `messages.index()` ==-collision-prone). `ConceptMapView.handleAskNora` gained the RC-FIX2 atomic dedup + in-flight ref + collision-proof `genId()` (was the only unguarded message writer, using `msg-${Date.now()}`). Removed pre-existing unused declarations (`App/LandingPage/PricingPage` React imports, `HelpCircle`, `startQuiz`, `planId`) that broke `tsc -b`.
- **Production Micro-SaaS Foundation (Phases 0–6 Roadmap & Core Infrastructure)**: Created full SaaS productization decision roadmap (launch track: landing, auth, DB, trial, payments, pricing — decisions logged in `NOTES.md` §2) and implementation plan. Built backend database architecture (`backend/db/database.py`, `backend/db/models.py`) with SQLAlchemy 2.0 async engine supporting Supabase Postgres and local SQLite. Created `User`, `Subscription`, `Lecture`, `UsageLog`, and `WebhookEvent` models. Implemented Supabase Auth JWT verification (`backend/auth.py`), Lemon Squeezy payment webhook handler with HMAC signature verification & idempotency logging (`backend/routers/webhooks.py`), startup DB initialization in `backend/main.py`, and free trial 15-minute video duration validation in `backend/orchestrator.py`.
- **Phase D of the NotebookLM parity roadmap: Mind Map / Concept Map per chapter & Tutor chat fixes.** Backend: added zero-LLM endpoint `GET /concept-map` (`backend/main.py`) deriving hierarchical 3-tier node/edge graphs from `lecture_outline.json` & `notes_chapter_<id>.json` at $0 token cost. Added section scoring & deduplication (`used_sections`), filtered markdown table delimiters (`| :--- |`), code fences (```), and stripped edge text labels. Frontend: `useChapterStore.ts` widened `activeDocTab` to include `'concepts'`; `DocPanel.tsx` added segment `07 Mind map`; new interactive `ConceptMapView.tsx` component with SVG bezier curved paths, tree/radial layout toggle, pan/zoom controls, floatable/draggable node detail card (`motion.div drag`), and streaming "Ask Nora about this concept" integration. `stripSources` regex updated in `ChatArea.tsx` & `MessageBubble.tsx` to eliminate raw source leakage, and `buildReferences` in `lib/references.ts` enhanced with text fallback parsing to guarantee `References N` count population. `vite.config.ts` proxy updated with `/concept-map`. Standalone test suite (`backend/test_api_contract.py`) expanded with `/concept-map` probe check.

- **Phase B of the NotebookLM parity roadmap: quiz & flashcard persistence + history UI.** Added per-lecture SQLite tables `quiz_attempts` & `flashcard_ratings` (`backend/main.py`) with zero-LLM metadata endpoints (`POST /quiz/attempts`, `POST /quiz/attempts/{id}/finish`, `GET /quiz/attempts`, `GET /quiz/attempts/{id}/missed`, `POST /flashcards/ratings`, `GET /flashcards/ratings`). Frontend: `useQuizStore.ts` tracks attempt state; `FlashcardsPanel.tsx` uses deterministic `SHA-256` content-hash keying (`lib/hash.ts`), persists rating updates to backend, and adds an "Again/Hard Missed Only" deck filter toggle; `QuizPanel.tsx` eval screen adds "Review Missed" action; `AssessmentView.tsx` adds a "Questions / History" segmented control with attempt history cards and "Retake Missed" buttons. Automated contract test suite (`backend/test_api_contract.py`) expanded to verify all 5 new endpoints.
- **Phase 3 of the NotebookLM parity roadmap: explain-with-citation + Study Guide (E).** New `POST /quiz/explain` (`backend/main.py`) resolves a question's source with a single on-demand retrieval (`tutor/retriever.retrieve`, top-1 note chunk + screenshot) — one embedding call, never regenerates; graceful `source:null` on a missing index. Frontend: `explainQuizQuestion` + shared `CitationBox` + `lib/cite.ts` click-to-scroll; "Explain · where is this in the notes?" in both `QuizPanel` and `AssessmentView` (`QuestionCard` `onExplain`). New `GET /study-guide` catalogs the already-generated per-chapter revision markdown into one doc (pure file reads); new `Guide` doc tab renders it via `StudyGuideView`, PDF maps to the revision print type. Contract test gained `/study-guide` + `/quiz/explain` probes.
- **Phase 2 of the NotebookLM parity roadmap: quiz difficulty filter (C).** `/quiz/questions` (`backend/main.py`) takes an optional `difficulty` (`Easy|Medium|Hard`, case-insensitive) that pre-filters the generated pool before the `n` sample — pure read, no regeneration (keeps cost posture). Frontend: `useQuizStore` forwards `difficulty`, and `startQuiz`/`retakeQuiz` persist/filter by the active difficulty; `AssessmentView` gained an `All/Easy/Medium/Hard` selector that refetches the list + Start Quiz and resets on lecture change. `backend/test_api_contract.py` gained a `difficulty=Easy` shape check. The plan's "bump pipeline default to 5" step was stale (prompts already target 8–10/chapter) and was dropped.
- **Phase 1 of the NotebookLM parity roadmap: tutor Study (Socratic) mode + custom persona.** Backend `ChatState` gained `study_mode` + `persona_instructions`; `tutor/prompts.py` added `SOCRATIC_SYSTEM_PROMPT` + `build_system_prompt(title, mode)`; `tutor/nodes.py` appends a persona `SystemMessage` when non-empty; `ChatRequest` + `invoke_tutor` thread both fields through `/chat` and `/chat/stream`. Frontend: `aiMode` widened to include `socratic`, new `Study` segment in the AI panel, per-lecture persona persists via `useTutorSettingsStore` (localStorage) and a `PersonaModal`. Delivered against the A–F NotebookLM-parity plan (A+F).
- **Backend ↔ frontend contract sync** (post-rebuild): `/quiz/questions` now returns `{"questions": [...], "incomplete": bool}`; removed dead `_load_quiz_questions` and orchestrator-local `update_lecture_title` shadow; `invoke_tutor` treats lecture id `default`/empty as the global graph. Frontend: options-driven generic question cards, `fetchQuizIncomplete` + `PartialContentBadge` in AssessmentView, `ProcessingPage` STAGES aligned to the real backend chain. Added standalone `backend/test_api_contract.py`.
- **Frontend design rebuild (Tier 0–5)**: "Architect's Sketchbook" theme; a11y/Web-Guidelines pass (Lighthouse a11y 100 both themes); signature typography pass; dark-mode "Luminous Blueprint at Night". Roadmap complete.
- **Tier 2 pipeline consolidation**: Files-API parallelization, chapter-batched visual extraction, merged chapter-artifact generation, screenshot selection remapping. ~72% call reduction.

## Pointers

- `AGENTS.md` — always-loaded instructions, commands, conventions.
- `README.md` — architecture + pipeline detail.
- `NOTES.md` — developer notes: pipeline fix log, pricing/decisions, open items.
- `NotebookLM_competitive_analysis.md` — forward feature ideas (flashcard persistence, quiz history).
- `audit/audit_tutor_and_auxiliary.md` — retained tutor/RAG/frontend audit detail.
