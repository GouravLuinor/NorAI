# NorAI — Developer Notes, Decisions & Lessons

Single place for the durable bits that outlived their original docs: the pipeline fix log (formerly `context.md`), the product/launch decisions (formerly `SAAS_ROADMAP.md`), and pointers to open work. Everything here cross-references live docs instead of duplicating them.

---

## 1. Pipeline Problem Registry & Fix Log

Historical bug → root-cause → fix table from the Tier 2/Tier 3 rebuild (kept because every row documents a real failure mode worth not repeating). Stage ordering in these rows predates the current 18-stage pipeline — the authoritative execution order lives in `backend/orchestrator.py` + `config.py`.

| # | Component / File | Issue Description | Root Cause | Fix Applied |
|---|---|---|---|---|
| 1 | `visual/visual_extractor.py` | `KeyError: 'path'` | Mismatch between mapper output (`"image_path"`) and extractor key (`"path"`). | Safe key lookup: `s.get("image_path") or s.get("path")`. |
| 2 | `notes/notes_generator.py` | `NameError: name 'types' is not defined` | Missing SDK import for `types.GenerateContentConfig`. | Added `from google.genai import types`. |
| 3 | `visual/visual_extractor.py` | `NameError: name 'create_empty_visual_object' is not defined` | Fallback function referenced on missing/failed visual objects but not written. | Defined `create_empty_visual_object(chunk_mapping)`. |
| 4 | `visual/visual_extractor.py` | `NameError: name 'ChapterVisualKnowledgeModel' is not defined` | Pydantic response schema class referenced in LLM call but omitted. | Defined `ChapterVisualKnowledgeModel` with `visual_objects: list[VisualObjectItem]`. |
| 5 | `notes/screenshot_selector.py` | Screenshot Pass 1 & Pass 2 URL Hallucination | Gemini Flash-Lite returned synthetic URLs (e.g. `https://storage.googleapis...`) instead of local paths, causing 100% frame rejection. | Added schema `Field(description=...)` hint and positional list remapping fallback in `score_frames_batch()` and `parse_selection()`. |
| 6 | `visual/visual_extractor.py` | Silent skip of all visual processing | `process_all_chunks()` looked for `"chunk_ids"` list, but outline chapters define ranges via `"start_chunk"` and `"end_chunk"`. | Derived range `list(range(start_chunk, end_chunk + 1))` when `"chunk_ids"` is absent. |
| 7 | `notes/notes_generator.py` | Missing fallback files on 3/3 retry failure | Retry degradation path wrote `chapter_N.md` but omitted revision, assessment, and flashcard fallback files. | Updated fallback logic to write incomplete stub files for all 4 artifact types. |
| 8 | `notes/screenshot_selector.py` | Missing `Field` import & `random` module reference bug | `Field` used without import (`NameError`), and `random.uniform()` called on imported `random` function object (`AttributeError`). | Updated imports to `from pydantic import BaseModel, Field` and `import random`. |
| 9 | `visual/visual_extractor.py` & `notes/screenshot_selector.py` | Duplicate Frame Uploads & Remapping Shift | Shared fallback keyframes appended per-chunk without deduplication caused duplicate Files API uploads & risk of index-based remapping shift. | Added canonical `dedup_paths()` helper, path deduplication before upload, filename-based remapping with collision guard. |
| 10 | `visual/visual_extractor.py` | Hallucinated `source_screenshots` URLs in Chapter JSONs | `source_screenshots` was generated by Gemini in batched extraction response_schema, producing synthetic URLs that propagated to chapter JSONs & failed screenshot selection. | Overrode `obj_dict["source_screenshots"] = chunk_image_map.get(vo.chunk_id, [])` in Python after LLM response parsing. |
| 11 | `backend/lecture_registry.py` | Lecture dropdown displayed "Untitled Lecture" | `lectures.json` title was not synced when outline generated title post-processing. | Added 3-tier fallback sequence in `list_lectures()` & `update_lecture_title()` post Stage 8. |
| 12 | `frontend/src/stores/useThreadStore.ts` | Static AI thread names (`Thread 1`) | AI chat threads remained default names. | Implemented auto-renaming helper on first question sent. |
| 13 | `visual/visual_extractor.py` & `notes/notes_generator.py` | `TypeError: ... not NoneType` on API 503 retry | `json.loads(response.text)` called when `response.text` evaluated to `None` on empty/blocked API responses. | Added explicit `if not response or not response.text: raise ValueError(...)` null guards. |
| 14 | Uvicorn Reloader | Server restarted mid-pipeline run (`1 change detected`) | Uvicorn watched output file writes in workspace. | Run with `--reload-exclude "outputs/*"`. |
| 15 | `chunking/chunk.py` & `notes/outline_generator.py` | Over-compressed 20-min lectures (only 2 chapters) | `DEFAULT_SEGMENTS_PER_CHUNK = 15` (3.75 mins/chunk) & aggressive `total_chunks // 3` capping formula. | Changed chunk size to `DEFAULT_SEGMENTS_PER_CHUNK = 5` (~1 min/chunk) & target 3–8 chapters. |
| 16 | `notes/notes_generator.py`, `backend/main.py`, `NotesView.tsx`, `PrintPage.tsx` | Inconsistent UI card rendering & raw JSON in PDF export | Free-form markdown relied on fuzzy regex `splitByH2` and string keyword searches. | Introduced Pydantic `StudyNoteSection` schema (`section_type`), saved `chapter_{id}.json` alongside `.md`, updated backend `/notes/{id}` & frontend/PrintPage rendering logic. |
| 17 | `transcription/transcribe.py` | Pipeline slower than "earlier builds" on an 8-min video (~14 min UI estimate) | (a) `bddaa9b` bumped whisper `base`→`small` (better WER but ~4× slower on CPU); (b) all LLM stages serialize through ONE global 12-RPM limiter (`bddaa9b` removed per-module limiters); (c) the 14-min figure was the UNCALIBRATED estimator default (`est_time_min`), not measured wall-clock. | Verified threads unchanged (extract=4, orchestrator=2). Measured baseline: transcription 104.4s, pipeline 399.6s (6.7 min) for an 8:26 video. Fix: `vad_filter=True` + `beam_size=1` (greedy) with `NORAI_WHISPER_VAD_FILTER`/`NORAI_WHISPER_BEAM_SIZE` env knobs; `backend/ratelimit.py` RPM now env-overridable via `NORAI_RPM_LIMIT`. After fix: transcription 60.7s (−42%), pipeline 207.2s (−48%), 15 LLM calls (was 19). |

---

## 2. Product & Launch Decisions

Decisions from the Micro-SaaS foundation work; the launch-track roadmap doc they lived in is retired (shipment complete).

**Base architecture (decided):** FastAPI backend (main + sync 18-stage pipeline in `backend/orchestrator.py`, run inside the DB-backed job queue `backend/jobs.py`) + React 19 / TypeScript Vite SPA. **Supabase Postgres** is the app DB (users, subscriptions, lectures, usage_logs, webhook_events); ChromaDB holds vectors; SQLite holds per-lecture LangGraph checkpoints under `outputs/<id>/tutor/`; artifact dirs + `outputs/lectures.json` on disk. Model default: `gemini-3.1-flash-lite`.

**Pricing tiers & quotas:**

| Tier | Price | Included Quota | Notes |
|---|---|---|---|
| **Free Trial** | $0 | 1 video (≤15 mins) | Full feature access on 1 lecture |
| **Starter** | $9 – $11 / mo | 5 Lecture-Hours / mo (~300 min, ≈15–20 lectures) | Full Pipeline, Flashcards, Quiz, Tutor |
| **Pro Student** | $23 – $29 / mo | 25 Lecture-Hours / mo (~1500 min, ≈80–100 lectures) | Priority processing, extended tutor history |

**Decision log (Q1–Q6):**
- **Q1 — App topology:** single unified Vite React 19 SPA (`frontend/`) for both landing and workspace.
- **Q2 — Target audience:** students & university course capture ("turn any lecture into study notes in seconds").
- **Q3 — Free-trial conversion:** full output access on 1 video (≤15 min) → paywall on video #2.
- **Q4 — Quota metric:** user-facing "Lecture Minutes / Hours" + internal Gemini token cost logging (metering in `backend/usage.py`).
- **Q5 — DB cutover:** clean-slate Postgres schema — no migration needed for gitignored `outputs/` dev data.
- **Q6 — Tutor threads schema:** per-lecture checkpoints on disk via LangGraph `AsyncSqliteSaver` (`outputs/<id>/tutor/checkpoints.sqlite`, WAL + busy_timeout) instead of centralized Postgres `user_threads`/`tutor_messages` tables — durable, isolated, trivially tied to lecture identity; threaded through the async graph cache.

**Stack choices:** Supabase Auth (JWT, ES256 via JWKS) · Lemon Squeezy as merchant of record (handles global VAT/GST; HMAC-SHA256 webhooks, fail-closed) · ChromaDB embeddings via `gemini-embedding-2` (768 dims).

---

## 3. Open Items

Forward-looking work — details live in the referenced docs:

- **Gemini paid-tier preflight (launch blocker).** Upgrade the Google AI Studio API from the free tier (15 RPM / 500 RPD) to pay-as-you-go Tier 1 before public launch.
- **P0.4a — frontend auth on reads (deferred).** Ownership scoping (`ensure_lecture_access`) only 404s foreign lectures when a token is presented; the frontend sends no token on artifact reads (`default` access). Deliberately not wired yet — see `ROADMAP.md` Phase P0.
- **Job-queue multi-worker claim-lock.** The DB-backed job queue currently assumes a single worker; running `--workers > 1` needs a claim-lock so two workers can't process the same lecture. See `backend/jobs.py`.
- **P6 retention (engineering backlog).** P6.1 token-level streaming, P6.2 SM-2 + Anki `.apkg` export, and P6.3 click-to-video grounding (chapter-level best-effort seek) are shipped; remaining: multi-lecture organization/sharing (P6.4), usage/cost dashboard (P6.5), exact-moment chunk-level video seeks — see `ROADMAP.md`.

---

## 4. Forward Feature Ideas

`NotebookLM_competitive_analysis.md` carries the forward moat ideas (flashcard history-driven study, quiz analytics, etc.) — consult it when scoping new features.